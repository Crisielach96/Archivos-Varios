#include <stdio.h>
#include <stdlib.h>
#include "funciones.h"

int a;
int b;

int main()
{
    sumar(a,b);
    restar(a,b);
    mult(a,b);
    division(a,b);
    factorial(a);
    return 0;
}
